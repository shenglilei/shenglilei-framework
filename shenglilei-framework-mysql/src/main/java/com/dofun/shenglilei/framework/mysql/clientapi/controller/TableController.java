package com.dofun.shenglilei.framework.mysql.clientapi.controller;

/**
 * 数据表管理
 */
//@Api(tags = "数据表管理服务(危险)")
//@RestController
//@Slf4j
//public class TableController implements TableInterface {
public class TableController {

//    @Resource
//    private TableService tableService;

//    @Autowired(required = false)
//    private DynamicTableNameProperties dynamicTableNameProperties;

//    @Override
//    public void split(TableSplitRequestParam param) {
//        log.info("表数据拆分，开始处理：{}", JSON.toJSONString(param));
//        if (dynamicTableNameProperties == null || !dynamicTableNameProperties.isEnabled()) {
//            log.info("功能配置为关闭，忽略处理");
//            return;
//        }
//        tableService.split(param);
//        log.info("表数据拆分，完成处理：{}", JSON.toJSONString(param));
//    }

//    @Override
//    public void merge(TableMergeRequestParam param) {
//        log.info("表数据合并，开始处理：{}", JSON.toJSONString(param));
//        if (dynamicTableNameProperties == null || !dynamicTableNameProperties.isEnabled()) {
//            log.info("功能配置为关闭，忽略处理");
//            return;
//        }
//        tableService.merge(param);
//        log.info("表数据合并，完成处理：{}", JSON.toJSONString(param));
//    }
}
